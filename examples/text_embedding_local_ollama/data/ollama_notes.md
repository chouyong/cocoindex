# Ollama Notes

Ollama exposes local models through an HTTP API running on localhost.

In this repository, the local embedding path uses the model nomic-embed-text through LiteLLM with the API base http://127.0.0.1:11434.

This file should match searches about local models, Ollama, embeddings, and API configuration.
