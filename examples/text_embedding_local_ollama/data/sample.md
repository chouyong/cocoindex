# CocoIndex Local Ollama Sample

This sample file exists to validate the local embedding path.

The example should read this markdown, split it into chunks, call the local Ollama embedding model, and write JSON files to the output directory.

Because the model runs on the same machine, this path does not require an external API key.
