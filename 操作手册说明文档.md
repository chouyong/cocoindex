# CocoIndex 本地操作手册

本文档面向当前目录下的 CocoIndex 项目，说明如何在 Windows PowerShell 环境里完成下载、安装、验证、运行与停止操作。

项目路径：

`D:\knowledgeBase\cocoindex\cocoindex_repo`

仓库地址：

`https://github.com/cocoindex-io/cocoindex.git`

## 一、项目简介

CocoIndex 是一个 `Python + Rust` 的增量数据处理框架。它的核心使用方式不是“先启动一个 Web 服务”，而是：

- 编写一个 CocoIndex App
- 使用 `cocoindex update` 执行该 App
- 如需持续监听变化，则使用 `cocoindex update --live`

因此，在本项目里，“启动服务”通常对应“启动某个 App 的持续运行模式”。

## 二、环境要求

建议本机具备以下环境：

- Python 3.11 及以上
- Rust 工具链
- PowerShell

当前这台机器已经验证过：

- Python 可用
- cargo 可用
- rustc 可用

## 三、首次下载安装

### 1. 克隆仓库

```powershell
cd D:\knowledgeBase\cocoindex
git clone https://github.com/cocoindex-io/cocoindex.git cocoindex_repo
```

### 2. 进入项目目录

```powershell
cd D:\knowledgeBase\cocoindex\cocoindex_repo
```

### 3. 安装本地开发版本

```powershell
pip install -e .
```

说明：

- 该命令会安装 Python 依赖
- 同时会构建项目所需的 Rust 扩展
- 安装成功后，本机就可以直接使用 `cocoindex` 命令

## 四、安装后验证

### 1. 查看版本

```powershell
cocoindex --version
```

### 2. 查看帮助

```powershell
cocoindex --help
```

如果看到 `init`、`ls`、`show`、`update`、`drop` 等命令，说明 CLI 已正常安装。

## 五、快速创建并运行一个项目

### 1. 初始化新项目

```powershell
cd D:\knowledgeBase\cocoindex\cocoindex_repo
cocoindex init quickstart_demo --dir quickstart_demo
```

### 2. 进入项目目录

```powershell
cd .\quickstart_demo
```

### 3. 执行 App

```powershell
cocoindex update .\main.py
```

成功后表示：

- 项目模板创建正常
- 本地 CLI 可正常驱动 CocoIndex App

## 六、运行官方示例

推荐先运行这个不依赖外部数据库或云服务的示例：

`examples\files_transform`

### 1. 进入示例目录

```powershell
cd D:\knowledgeBase\cocoindex\cocoindex_repo\examples\files_transform
```

### 2. 安装示例依赖

该示例通常还需要：

```powershell
pip install "markdown-it-py[linkify,plugins]"
```

### 3. 执行示例

```powershell
cocoindex update .\main.py
```

### 4. 查看输出

输出目录：

`D:\knowledgeBase\cocoindex\cocoindex_repo\examples\files_transform\output_html`

如果目录下出现 `.html` 文件，说明示例已经运行成功。

## 七、以 live 模式持续运行

如果希望在源文件变化时自动重新处理，可以运行：

```powershell
cd D:\knowledgeBase\cocoindex\cocoindex_repo\examples\files_transform
cocoindex update .\main.py --live
```

效果：

- 首次会先做一次全量或增量处理
- 完成后不会立即退出
- 会持续监听输入目录变化
- 检测到变化后自动重新处理

## 八、后台启动方式

如果不希望前台窗口被占用，可以用 PowerShell 后台启动：

```powershell
Start-Process -FilePath "D:\miniconda3\Scripts\cocoindex.exe" `
  -ArgumentList "update",".\main.py","--live" `
  -WorkingDirectory "D:\knowledgeBase\cocoindex\cocoindex_repo\examples\files_transform"
```

建议同时记录：

- 启动时间
- 进程 PID
- 工作目录
- 日志路径

## 九、日志位置

如果采用带重定向的后台启动方式，可把日志写入：

- 标准输出：`cocoindex-live.stdout.log`
- 标准错误：`cocoindex-live.stderr.log`

本次实际使用的日志位置为：

- `D:\knowledgeBase\cocoindex\cocoindex_repo\examples\files_transform\cocoindex-live.stdout.log`
- `D:\knowledgeBase\cocoindex\cocoindex_repo\examples\files_transform\cocoindex-live.stderr.log`

## 十、如何停止运行中的进程

如果已经知道 PID：

```powershell
Stop-Process -Id 31648
```

如果不知道 PID，可先查询：

```powershell
Get-Process | Where-Object { $_.ProcessName -eq 'cocoindex' }
```

再执行：

```powershell
Stop-Process -Id <PID>
```

## 十一、常见问题

### 1. 为什么找不到 `serve` 或 `server` 命令

因为当前仓库根 CLI 主要是面向本地 App 运行，不是标准 Web 服务项目。当前重点命令是：

- `cocoindex init`
- `cocoindex update`
- `cocoindex update --live`

### 2. 为什么示例运行时报缺少依赖

有些示例目录带有自己的 `pyproject.toml`，包含额外依赖。遇到报错时，优先查看：

- 仓库根 `pyproject.toml`
- 当前示例目录下的 `pyproject.toml`

### 3. 下载慢怎么办

本机如果已经有代理服务，可按需设置：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:端口"
$env:HTTPS_PROXY="http://127.0.0.1:端口"
```

设置前应先确认代理端口真实可用。

### 4. `uv` 没有安装怎么办

对于本仓库的本地安装，不一定必须先安装 `uv`。当前已经验证：

```powershell
pip install -e .
```

即可完成可用安装。

## 十二、本机 Ollama 使用说明

如果后续需要在 CocoIndex 项目中测试本地 embedding、本地模型或 `LiteLLM + Ollama`，这台机器已经有一套可用的 `Ollama` 环境，优先直接复用。

### 1. 当前状态

已确认：

- `ollama --version` 正常，版本为 `0.24.0`
- `ollama list` 正常
- 本地接口 `http://127.0.0.1:11434/api/tags` 正常
- 当前已验证存在模型：`nomic-embed-text:latest`

### 2. 本机实际目录

这台机器上的 Ollama 已迁移到 D 盘，当前约定如下：

- 程序目录：`D:\knowledgeBase\Ollama`
- 模型与数据目录：`D:\knowledgeBase\.ollama`
- 日志与本地状态目录：`D:\knowledgeBase\OllamaLocal`
- `ollama.exe` 实际位置：`D:\knowledgeBase\Ollama\ollama.exe`

兼容原安装路径的目录联接也已经建立：

- `C:\Users\zhouy\AppData\Local\Programs\Ollama -> D:\knowledgeBase\Ollama`
- `C:\Users\zhouy\.ollama -> D:\knowledgeBase\.ollama`
- `C:\Users\zhouy\AppData\Local\Ollama -> D:\knowledgeBase\OllamaLocal`

因此，后续一般不要重复安装，也不要重新下载已经存在的模型。

### 3. 常用检查命令

```powershell
ollama --version
ollama list
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:11434/api/tags
```

如果需要补 embedding 模型，使用：

```powershell
ollama pull nomic-embed-text
```

### 4. 与 CocoIndex 一起使用时的建议

如果示例或实验代码要走本地 embedding，可优先用：

- 模型名：`ollama/nomic-embed-text`
- 本地地址：`http://127.0.0.1:11434`

这类本地 Ollama 调用通常不需要 API key。

### 5. 代理注意事项

如果当前只是访问本地 `Ollama`，通常不需要代理。

如果同时还要联网安装依赖，可按需设置：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:18080"
$env:HTTPS_PROXY="http://127.0.0.1:18080"
$env:NO_PROXY="127.0.0.1,localhost"
```

这样可以避免本地 `11434` 请求被错误转发到代理。

## 十三、最小本地 Ollama 示例

仓库里现在已经提供了一个可以直接跑通的最小示例：

- 目录：`D:\knowledgeBase\cocoindex\cocoindex_repo\examples\text_embedding_local_ollama`
- 作用：读取本地 Markdown，调用本地 `Ollama` 生成 embedding，并把结果写到本地 JSON 文件
- 附加能力：基于这些 JSON 向量直接做本地相似度查询

### 1. 这个示例为什么适合作为首选联调入口

相比 Postgres、Qdrant 一类 embedding 示例，这个版本：

- 不依赖 Postgres
- 不依赖 Qdrant
- 不依赖外部 API key
- 只依赖本地 `Ollama` 和 `litellm`

因此更适合先验证“本机模型链路是否可用”。

### 2. 示例默认行为

入口文件：

`examples\text_embedding_local_ollama\main.py`

默认读取：

`examples\text_embedding_local_ollama\data`

默认输出：

`examples\text_embedding_local_ollama\output_embeddings`

默认本地状态库：

`examples\text_embedding_local_ollama\cocoindex.db`

默认模型配置：

- `OLLAMA_API_BASE=http://127.0.0.1:11434`
- `OLLAMA_EMBED_MODEL=ollama/nomic-embed-text`

### 3. 首次运行

如果当前 Python 环境还没有 `litellm`，先安装：

```powershell
pip install litellm
```

然后执行：

```powershell
cd D:\knowledgeBase\cocoindex\cocoindex_repo\examples\text_embedding_local_ollama
cocoindex update .\main.py
```

### 4. 已验证联调结果

本次已实际跑通，结果如下：

- `cocoindex update .\main.py` 成功完成
- 生成输出文件：`output_embeddings\data__sample.md__0_334.json`
- 输出 JSON 中已确认包含：
  - `embedding_model = ollama/nomic-embed-text`
  - `ollama_api_base = http://127.0.0.1:11434`
  - `embedding_dim = 768`
- 输出向量为真实浮点数组，不是空值
- 后续单次查询命令也已跑通：

```powershell
python .\main.py "local embedding path"
```

- 当前实际返回的 Top 1 结果是：`data\sample.md`
- 当前实际返回的分数是：`0.7575`

### 5. 二次运行验证

同一输入再次执行：

```powershell
cocoindex update .\main.py
```

已看到：

- `process_file: 1 unchanged`

这说明：

- CocoIndex 本地状态库有效
- memo 链路有效
- 不会对未变化文件重复做 embedding

### 6. 本地相似度查询怎么用

先确保已经执行过：

```powershell
cocoindex update .\main.py
```

然后可用两种查询方式。

单次查询：

```powershell
python .\main.py "local embedding path"
```

交互式多轮查询：

```powershell
python .\main.py
```

查询链路是：

- 先调用本地 `Ollama` 为查询词生成 embedding
- 再读取 `output_embeddings\*.json`
- 在本地计算 cosine similarity
- 打印 Top K 结果

本次交互式模式也已实际验证：

- 输入 `local embedding path`，返回 `data\sample.md`
- 输入 `CocoIndex sample`，返回 `data\sample.md`
- 空行可以正常退出

### 6. 演示版新增能力

这个示例现在已经不只是“最小可跑通”，而是一个可演示版本，新增了：

- 交互式多轮查询
- 显示 Top K 可配置
- 支持多个 Markdown 文件共同参与检索

当前示例目录下已放入 4 个 Markdown 文件，重建后已生成 4 个 JSON embedding 块。

### 7. Top K 怎么配

单次查询时可直接指定：

```powershell
python .\main.py --query "local model embeddings" --top-k 3
```

交互式模式启动时也可指定：

```powershell
python .\main.py --top-k 3
```

进入交互模式后，还可以临时修改：

```text
/top 2
```

### 8. 多文件检索已验证结果

本次已实际验证：

- 单次查询 `local model embeddings` 时，输出：
  - `Showing top 3 of 4 indexed chunks`
  - Top 1: `data\ollama_notes.md`
  - Top 2: `data\sample.md`
  - Top 3: `data\vector_search_demo.md`

- 交互式模式中：
  - `/top 2` 可正常生效
  - 查询 `vector search` 时，Top 1 命中 `data\vector_search_demo.md`
  - 查询 `Ollama API` 时，结果中包含 `data\ollama_notes.md`

### 9. 按文件名过滤结果

如果只想在部分文件里检索，可加文件名过滤：

```powershell
python .\main.py --query "local model embeddings" --file-contains ollama
```

这会只保留源文件路径中包含 `ollama` 的 JSON 向量块参与排序。

本次已实际验证：

- 输出 `Showing top 1 of 1 indexed chunks`
- 输出 `File filter: ollama`
- Top 1 命中 `data\ollama_notes.md`

### 10. 导出 Markdown 报告

单次查询时可直接导出报告：

```powershell
python .\main.py --query "local model embeddings" --top-k 3 --file-contains ollama --report .\demo-report.md
```

已实际生成：

- `examples\text_embedding_local_ollama\demo-report.md`

交互式模式也支持导出：

```text
/report .\interactive-report.md
```

随后每次查询都会把当前结果导出到该 Markdown 文件，直到执行：

```text
/clearreport
```

本次已实际生成：

- `examples\text_embedding_local_ollama\interactive-report.md`

### 11. 查询结果高亮片段

这个演示版现在还会在结果预览里高亮查询词：

- 终端输出中使用 `[[关键词]]`
- Markdown 报告中使用 `**关键词**`

例如：

- 查询 `Ollama API` 时，`data\ollama_notes.md` 的终端预览里会出现 `[[Ollama]]` 和 `[[API]]`
- 查询 `vector search` 并导出报告时，报告中的高亮预览会出现 `**Vector**` 和 `**Search**`
- 本次已实际生成：
  - `examples\text_embedding_local_ollama\highlight-report.md`

这部分高亮的作用是帮助快速读结果，不影响相似度排序本身。

需要注意：

- 它是基于查询词的字面匹配做高亮
- 它不是 embedding 模型内部 attention 或 token 级解释

### 12. 静态 HTML 查询报告页

这个演示版现在还支持把查询结果导出成一个静态 HTML 报告页。

它不是本地 Web 服务，也不是单独的 UI 进程，而是：

- 一次查询后直接生成一个 `.html` 文件
- 用浏览器本地打开即可查看
- 适合把当前查询结果留档或发给别人看

单次查询导出方式：

```powershell
python .\main.py --query "vector search" --top-k 2 --html-report .\report.html
```

本次已实际验证：

- 命令成功执行
- 终端输出：
  - `Showing top 2 of 4 indexed chunks`
  - Top 1: `data\vector_search_demo.md`
  - Top 2: `data\cocoindex_overview.md`
  - `HTML report written to report.html`
- 已生成文件：
  - `examples\text_embedding_local_ollama\report.html`

交互式模式里也支持启用 HTML 导出：

```text
/htmlreport .\interactive-report.html
```

之后每次查询都会把当前结果写到这个 HTML 文件，直到执行：

```text
/clearhtmlreport
```

本次交互式模式也已实际验证生成：

- `examples\text_embedding_local_ollama\interactive-report.html`

当前这个 HTML 报告页包含：

- Query
- Top K
- Indexed Chunks Searched
- Matching Chunks After Filter
- File Filter
- Ollama API Base
- Embed Model
- Embedding JSON Directory
- 每条结果的 score、chunk 范围、embedding 维度
- 带高亮的预览片段
- 可展开的完整原文

其中高亮在 HTML 里使用 `<mark>` 样式显示，用来帮助快速扫描命中内容，不改变向量相似度排序。

### 13. 本地小型 Web UI

如果不想每次都看终端输出，也不只想导出静态 HTML 文件，现在这个示例还支持一个本地小型 Web UI。

启动方式：

```powershell
python .\main.py --web-ui
python .\main.py --web-ui --port 8008
```

默认会绑定到：

- `http://127.0.0.1:8008`

这个 Web UI 的特点是：

- 仍然只用本地 `Ollama`
- 仍然只读本地 `output_embeddings\*.json`
- 查询时实时做本地 cosine similarity
- 不需要 Postgres
- 不需要 Qdrant
- 不需要远端 API key

页面里当前提供：

- Query 输入框
- Top K 输入框
- File Filter 输入框
- File Filter 的本地文件名下拉候选
- 查询参数摘要卡片
- Indexed Chunks 与 Matching Chunks 统计
- 当前结果直接下载为 Markdown 或 HTML 的按钮
- 高亮预览片段
- 可展开查看全文

本次已实际完成联调验证：

- 用线程方式启动了 `launch_web_ui(...)`
- 实际请求首页：
  - `http://127.0.0.1:8016/`
- 实际请求查询页：
  - `http://127.0.0.1:8016/?query=vector+search&top_k=2&file_contains=vector`
- 已确认页面响应中包含：
  - `Local Ollama Web UI`
  - `data\vector_search_demo.md`
  - `Matching Chunks`
  - `<mark>Vector</mark>`

因此可以把它看成一个比静态 HTML 报告页更适合本机交互的入口：

- `--html-report` 更适合导出报告文件
- `--web-ui` 更适合临时浏览器查询

### 14. Web UI 导出按钮与文件名下拉候选

这个 Web UI 现在进一步补了两个更实用的能力：

- 导出当前结果为 Markdown / HTML 按钮
- 按文件名过滤时的下拉候选
- 点击结果卡片里的文件名标签后自动套用过滤
- 下载文件名自动带 query 和时间戳

其中下拉候选来自：

- `output_embeddings\*.json` 里当前已索引的 `source_file`

所以用户在页面上不需要手敲完整文件名，也不用先记住有哪些 Markdown 文件。

本次已实际验证：

- 首页请求：
  - `http://127.0.0.1:8018/`
- 页面里已确认出现：
  - `file-filter-options`
  - `data\vector_search_demo.md`
  - `Indexed source files`

查询页请求：

- `http://127.0.0.1:8018/?query=vector+search&top_k=2&file_contains=vector`

页面里已确认出现：

- `Download Markdown`
- `Download HTML`
- `Matching Chunks`
- `<mark>Vector</mark>`

当前下载按钮会按当前查询参数直接返回文件内容，不要求先在命令行里指定输出路径。

本次已实际验证下载接口：

- Markdown 下载：
  - `http://127.0.0.1:8018/?query=vector+search&top_k=2&file_contains=vector&download=md`
- HTML 下载：
  - `http://127.0.0.1:8018/?query=vector+search&top_k=2&file_contains=vector&download=html`

已确认 Markdown 下载内容中包含：

- `# Local Ollama Query Report`
- `File filter: \`vector\``
- `Matching chunks after filter: \`1\``

已确认 HTML 下载内容中包含：

- `Local Ollama Query Report`
- `Matching Chunks After Filter`
- `data\vector_search_demo.md`

另外，这次也顺手修了一个 Web UI 内部稳定性问题：

- 现在 Web UI 使用一个常驻的 asyncio event loop 来复用查询 embedding 调用
- 已避免每次 HTTP 请求单独 `asyncio.run(...)` 时可能触发的 `Event loop is closed`

本次还额外验证了两个细节：

- 查询页 `http://127.0.0.1:8019/?query=vector+search&top_k=2` 中，结果卡片会出现：
  - `filter:data\vector_search_demo.md`
- 访问对应过滤 URL：
  - `http://127.0.0.1:8020/?query=vector+search&top_k=2&file_contains=data%5Cvector_search_demo.md`
  - 页面中已确认出现：
    - `Current filter: <code>data\vector_search_demo.md</code>`
    - `Matching Chunks`

下载文件名方面，本次响应头已实际确认包含：

- `attachment; filename="local-ollama-query-vector-search-20260517-191126.md"`
- `attachment; filename="local-ollama-query-vector-search-20260517-191126.html"`

本次又继续补了两层过滤态交互：

- 当前过滤结果数的单独徽标
- 点击该徽标后切换到“全量结果对比视图”

实际验证地址：

- 过滤页：
  - `http://127.0.0.1:8023/?query=vector+search&top_k=2&file_contains=data%5Cvector_search_demo.md`
- 对比页：
  - `http://127.0.0.1:8023/?query=vector+search&top_k=2&file_contains=data%5Cvector_search_demo.md&compare=all`

已确认过滤页里出现：

- `Compare With All Results`
- `Filtered Results:`
- `Clear Current Filter`

已确认对比页里出现：

- `All Results Baseline`
- `Showing the same query without the current file filter`
- `Current filter: <code>data\vector_search_demo.md</code>`

这意味着现在的过滤交互已经形成一套完整链路：

- 点击结果文件标签进入过滤态
- 过滤态里可看到当前结果数徽标
- 点击徽标可打开“当前过滤结果 vs 全量结果”的同页对比视图
- 如需退出过滤，只用点 `Clear Current Filter`

### 15. warning 优化处理

之前本地联调时出现过两个常见 warning：

1. `LiteLLM` 尝试拉远端 `model cost map`，超时后回退本地备份
2. 因缺少 `botocore`，出现 Bedrock / SageMaker 相关预加载 warning

当前已经做了对应优化：

- 在示例代码里设置 `LITELLM_LOCAL_MODEL_COST_MAP=true`
- 本机已安装 `botocore`

当前这两个 warning 已不再作为该示例的常规噪音出现。

### 16. 一个已确认的安装坑

当前机器上，下面这个命令不一定稳：

```powershell
pip install -e ".[litellm]"
```

原因是 editable 构建时可能遇到：

- `python\cocoindex\_internal\core.pyd` 被占用

如果遇到这种情况，当前更稳妥的做法是：

```powershell
pip install litellm
```

这样可以直接补齐本地 Ollama 示例所需依赖，而不必重装整个 editable 包。

## 十七、推荐的最小操作流程

如果只是想快速确认本项目可以正常运行，建议按下面顺序执行：

```powershell
cd D:\knowledgeBase\cocoindex\cocoindex_repo
pip install -e .
cocoindex --version
cd .\examples\files_transform
pip install "markdown-it-py[linkify,plugins]"
cocoindex update .\main.py
```

如果 `output_html` 目录生成了 HTML 文件，就说明项目已经具备本地可运行能力。
